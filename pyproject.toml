[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tempo"
version = "0.1.0"
description = "A chess helper that grows with you instead of playing at a fixed ELO."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
